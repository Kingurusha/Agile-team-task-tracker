# old one (is functional?)
#spring:
#  application:
#    name: testName
#  datasource:
#    url: jdbc:h2:mem:testdb
#  h2:
#    console:
#      enabled: true

server:
  servlet:
    session:
      timeout: 30m

# database connection setup
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: fasdf
    username: fdfd
    password: sdfasd
